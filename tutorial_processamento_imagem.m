%uma imagem é uma funcao bidimensional f(x,y) = z
%precisamos amostrar os dados para armazenar no PC
%para armazenar uma imagem com cor precisamos de 3 parametros
%para armazenar uma imagem em PB precisamos de só um parametro que
%representa a intensidade
%bitmap = resoluçao*3
% A = imread('lena.bmp');
% size(A);
% imshow(A)
% 
% %virar uma imagem de cabeca para baixo
% U = flipud(A);
% imshow(U);
% 
% %virar uma imagem no sentido horizontal
% L = fliplr(A);
% imshow(L);
% 
% %virar das duas formas de uma vez só
% UL = flipud(fliplr(A));
% imshow(UL);
% 
% %desativando as componentes vermelha e verde
% B = A;
% %primeiro argumento respeito as linhas, segundo as colunas e o 1 a cor
% %vermelha
% B(:,:,1) = 0;
% %o mesmo foi feito para a cor verde
% B(:,:,2) = 0;
% imshow(B);
% %imshow(B(:,:,1));




